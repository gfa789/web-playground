specVersion: "v1beta"
serviceId: "playground"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "playground-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
